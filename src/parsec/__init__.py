"""Parsec: local-first research-agent harness.

The durable core is the data model (evidence DAG, event log, fetch cache);
the loop is deliberately thin and replaceable (T7).
"""

__version__ = "0.1.0"
